name: CI

on:
  push:
    branches: [ main, master ]
  pull_request: {}   # toutes les branches/PR

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  # ⚡ Feedback rapide : format + clippy (Linux stable)
  lint:
    name: Lint (fmt + clippy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: rustfmt check
        run: cargo fmt --all -- --check

      - name: clippy (deny warnings)
        run: cargo clippy --all-targets --all-features -- -D warnings

  # 🧪 Build & tests multi-OS + variations de features
  test:
    name: Test (${{ matrix.os }} • ${{ matrix.toolchain }} • f=${{ matrix.features }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-14, windows-latest ]
        toolchain: [ stable ]
        features: [ "", "checksum", "compress", "checksum,compress" ]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (${{ matrix.toolchain }})
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.toolchain }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Compute feature args
        shell: bash
        run: |
          if [ -n "${{ matrix.features }}" ]; then
            echo 'FEATURES=--features "${{ matrix.features }}"' >> "$GITHUB_ENV"
          else
            echo 'FEATURES=' >> "$GITHUB_ENV"
          fi

      - name: Build (release)
        shell: bash   # pour que $FEATURES marche aussi sur Windows
        run: cargo build --release --locked --no-default-features $FEATURES

      - name: Test
        shell: bash
        run: cargo test --all --locked --no-default-features $FEATURES

  # ⛓️ MSRV : aligne avec ton workspace (1.80.0)
  msrv:
    name: MSRV (1.80.0)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (MSRV)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.80.0

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build (no default features)
        run: cargo build --locked --no-default-features

      - name: Test (no default features)
        run: cargo test --locked --no-default-features

  # 🧹 Vérifs supplémentaires (doc + examples) sur Linux
  extras:
    name: Extras (doc, examples)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Doc build (no-deps, deny warnings)
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps

      - name: Build examples (non-blocking)
        shell: bash
        run: cargo build --examples || true
