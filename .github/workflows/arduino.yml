
name: arduino-microvitte
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Arduino CLI setup (placeholder)
        run: |
          echo "Install arduino-cli and cores here"
          echo "arduino-cli compile --fqbn arduino:avr:uno microvitte/examples/HelloVitte"
