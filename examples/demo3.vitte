# Imports namespacés + comp avancée (double for)
import "examples/math.vitte" as m

def main():
  xs = [1,2,3]
  ys = [10,20]
  zs = [x + y for x in xs for y in ys]   # comp double
  print(zs)                               # [11,21,12,22,13,23]
  print(m.square(5))                      # 25
  xs.insert(1, 99); print(xs)             # [1,99,2,3]
  xs.remove(1); print(xs)                 # [1,2,3]
main()
