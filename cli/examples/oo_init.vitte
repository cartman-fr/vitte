# EXPECT: Ada
# EXPECT: Hello, Ada!
class User {
  static domain: "example.com"
  name: "unknown"
  greet(self) -> print("Hello, " + self.name + "!")
  init(self) -> self.name = "Ada"
}
u = new User {}
print(u.name)
u.greet()
